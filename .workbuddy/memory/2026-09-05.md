# 2026-09-05 工作日志

## 自动化任务执行：Day7 学习计划

### 当前状态盘点

**已完成技术资产**（共37个Python源文件，~9,747行代码）：
- ① 分类：classifier.py + 规则+LLM兜底
- ② 提取：extractor.py + 字段级兜底
- ③ 决策：agent.py + 纯规则
- ④ RAG检索：retriever.py + vector_retriever.py + reranker.py（三重递进，P@1=95%）
- ⑤ RAG生成：context_builder.py + generator.py（引用准确率100%，忠实度85.7%）
- ⑥ Agent引擎：agent_loop.py + multi_agent.py + memory_store.py + state_graph.py + mcp_client/server.py + llm_service.py
- ⑦ 飞书系统：运维案例（40人使用，5条自动化）

**评测数据现状**：
| 文件 | 样本数 | 关键指标 |
|---|---|---|
| eval_agent.json (r10) | 20 | A=20%, B=95%, B成本¥0.0332/条 |
| eval_agent_r6.json | 20 | A=55%, B=70%, B降级15次 |
| eval_query_optimization.json | 20 | A/B/C三种query构造均100%命中 |
| eval_rerank.json | 20 | P@1=95%, NDCG@3=0.908 |

**关键发现**：
- max_rounds=10时Agent表现最佳（95%可用率），max_rounds=6时降级严重（15/20）
- Pipeline在r6测试中55%可用率，但远未发挥query优化潜力
- query优化实验显示三种构造方式都能实现100%检索命中率，差异在生成阶段

**阻塞点**：
- DeepSeek余额可能已耗尽（需验证）
- Pipeline query优化尚未正式落地

---

## Day7 计划：Pipeline Query优化 + A/B对比验证

### 任务清单

**必做（2项）**：

① **Pipeline query优化落地**（60min）
- 修改 `src/workflow.py::_build_query()` 或等效函数
- 从 `f"{customer} {product}"` 升级为 `f"{customer} {product} {subject} {intent_terms}"`
- 意图词从 eval_query_optimization.py 的 INTENT_TERMS 列表选取
- 落地后重新跑 eval_pipeline 验证可用率提升

② **统一口径A/B对比重跑**（60min）
- 使用 eval_agent.py，统一 max_rounds=10
- Pipeline侧使用优化后query（B_subject或C_rich模式）
- Agent侧保持原有逻辑
- 对比两组有用率、成本、耗时
- 输出新 eval_final_comparison.json

**可选（2项）**：

③ **README更新**（30min）
- 新增「Pipeline query优化」一节
- 更新效果数据表格
- 更新架构图

④ **dist更新部署**（30min）
- 更新 dist/index.html 效果数据
- 重新部署到 workbuddy.link

---

### 预期成果

1. Pipeline可用率从当前约20-55%提升至70%+
2. 形成完整对比报告，证明「query优化是关键突破口」
3. README和dist同步更新

---

### 执行约束

- 不增加新的Python依赖
- 复用已有INTENT_TERMS列表
- 成本优先，控制在¥1.0以内（20条样本）
- 若DeepSeek余额不足，先用eval_query_optimization.py验证检索层效果（零成本）

---

### 进度判断

- 目标岗位：AI应用工程师（JD十项技能）
- 已完成：①分类 ②提取 ③决策 ④RAG检索 ⑤RAG生成 ⑥Agent引擎 ⑦飞书案例
- 今日推进：Pipeline优化（JD #2 Agent引擎的深化理解）
- 剩余方向：⑧MCP服务封装 ⑨Docker化部署 ⑩系统集成
