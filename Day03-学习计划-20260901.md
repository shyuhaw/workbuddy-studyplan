# Day 3 学习计划 · 2026-09-01（周二 · 工作日）

> **今日主题**：**RAG Day 1 —— 建 BM25 baseline**（四周计划 W1-2 主轴，补最大空白）  
> **时间预算**：下班后 **1.5 小时**  
> **今日产出**：客户档案语料 + BM25 检索器 + 检索评测集 + 第一份 baseline 数字

---

## 〇、结论先行（30 秒看完）

### Day2 对账：计划全没做，但**方向做对了**

| Day2 计划项         | 实际   | 处置                                                    |
| ---------------- | ---- | ----------------------------------------------------- |
| 封装 3 个自定义 Skill  | ❌ 没做 | 🔻 **降级到周末**（30min 一次性清空，别一直欠着）                       |
| 效率对比表            | ❌ 没做 | 🔻 **降级到周末**（README 已有成本/效果数据，只缺"人工vs AI耗时"，10min 能填） |
| 飞书→WorkBuddy 映射表 | ❌ 没做 | ❌ **砍掉**（只对 8-9K WorkBuddy 岗有用，对 15-25K 岗零价值）         |

**你把时间砸在了项目上，产出了**：端到端 Agent v1.0 + 真实邮件接入 + 作品集部署上线 + 定时任务建立。  
**这个自发取舍是对的**——一个做深的项目 > 三份表格文档。计划赶不上你对价值的判断。

### 今天的判断

| 优先级   | 任务                          | 理由                    |
| ----- | --------------------------- | --------------------- |
| 🔴 主轴 | **RAG Day 1：BM25 baseline** | JD 排第 1 的技能，你唯一完全空白的项 |
| 🟡 顺手 | 确认今早 9:01 定时任务是否出活          | L2「异常处理」的关键验证点        |
| ⚪ 不做  | 装 LangChain / Chroma        | **今天不要碰框架**，见下方红线     |

---

## 一、为什么先做 BM25，而不是向量检索

这是今天最重要的一个决策，想清楚再动手：

| # | 理由                                                                                |
| - | --------------------------------------------------------------------------------- |
| 1 | **没有 baseline 就证明不了提升**。JD 原话要的是"混合检索比纯向量检索召回率提升 X%"——你今天把 BM25 数字先钉死，明天加向量才有对比对象 |
| 2 | **BM25 是你已有能力的自然延伸**。你规则层玩的就是关键词+权重，BM25 是它的概率化升级版，不换脑子                           |
| 3 | **零依赖、1.5h 能跑完**。向量检索要选模型、申请 Key、处理网络，塞不进工作日晚上                                    |

> 💡 **反面教材**：大部分人学 RAG 第一件事是 `pip install langchain chromadb`，  
> 两天后还在调环境，一行检索逻辑都没写。**你不是来学框架的，是来拿数字的。**

**明天再做向量**：已替你查证——智谱 `embedding-3`，**0.5 元/百万 tokens**，  
OpenAI 接口兼容（`https://open.bigmodel.cn/api/paas/v4/embeddings`），支持 256-2048 自定义维度。  
（DeepSeek **没有** embedding 接口，别白找。）

---

## 二、时间块（下班后 1.5h）

| 时段          | 时长    | 任务                             | 产出                |
| ----------- | ----- | ------------------------------ | ----------------- |
| 20:00-20:05 | 5min  | ① 确认定时任务是否出活                   | 一句结论              |
| 20:05-20:20 | 15min | ② RAG 五环节骨架认知                  | 能口述链路             |
| 20:20-21:05 | 45min | ③ **建客户档案语料 + BM25 检索器** 🔴    | 2 个 py + 1 个 json |
| 21:05-21:25 | 20min | ④ **建评测集 + 跑出 baseline 数字** 🔴 | Recall@K / MRR    |
| 21:25-21:30 | 5min  | ⑤ 记忆写入                         | 日志更新              |

---

## 三、任务 ① 确认定时任务是否出活（5 分钟）

「每日外贸邮件智能处理」今天 **9:01 首次运行**。

**📋 可复制指令**：

```
帮我确认今早 9:01 的「每日外贸邮件智能处理」定时任务有没有正常执行。
检查 output 目录有没有新文件。
如果没有新文件，判断是「收件箱无新邮件」（正常）还是「任务失败」（要修），
并明确告诉我结论和依据。
```

> 🎯 这一步的意义：定时任务的五要素里，**异常处理和日志**是 L2 能不能用起来的分水岭。  
> 第一次跑就要确认清楚，否则它会静默失败三个月你都不知道。

---

## 四、任务 ② RAG 五环节骨架（15 分钟）

只要记住这张表，面试时"讲讲 RAG 项目"就能接住：

| 环节          | 做什么         | 今天的做法                          |
| ----------- | ----------- | ------------------------------ |
| 1. 文档解析     | 把原始资料变成纯文本  | ✅ 你已有 `clean_body()`，直接复用      |
| 2. 切分 Chunk | 按语义单元切块     | 一封往来 = 一个 chunk（**天然切分，不用算法**） |
| 3. 向量化      | 文本 → 向量     | 今天跳过（BM25 是稀疏表示，不需要）           |
| 4. 检索       | 找最相关的 chunk | **BM25**（今天的核心）                |
| 5. 组装生成     | 拼上下文给 LLM   | 明天做                            |

### ⚠️ 今天场景的关键设计：为什么是「客户档案」

不是给一堆文档做问答（那是玩具），而是给**邮件 Agent 加一层记忆**：

```
新邮件进来 → 检索「这个客户的历史往来」→ 组装上下文 → 辅助分类/提取/决策
```

**真实业务价值**（面试可讲的三条）：

| # | 价值                              |
| - | ------------------------------- |
| 1 | 老客户报价格式可对照 → 识别"客户是不是在压价"       |
| 2 | 历史索赔记录 → 同一客户再投诉时自动升级优先级        |
| 3 | **数据飞轮**：每处理一封邮件就往档案追加一条，档案越用越准 |

这是从「**单封邮件理解**」到「**客户关系理解**」的跃迁——单独的分类/提取模块永远做不到。

---

## 五、任务 ③ 建语料 + BM25 检索器（45 分钟）🔴 核心

### 5.1 装依赖（2 分钟）

沿用你本机的既定约定（`pip --target`，venv 在这台机器创建会失败）：

```bash
"C:/Users/Administrator/AppData/Local/Programs/Python/Python311/python.exe" -m pip install --target "C:/Users/Administrator/.workbuddy/binaries/python/pkgs" rank_bm25
```

> `rank_bm25` 是单文件纯 Python 实现，装完不到 100KB，不污染环境。

### 5.2 建客户档案语料（15 分钟）

**数据来源**：把已有的 4 个数据集合并按客户聚合（16 封样本 + 6 封歧义 + 10 封 e2e + 3 封真实邮件）。

**📋 可复制指令**：

```
我要给邮件 Agent 加一层「客户历史档案」检索。第一步：建语料。

写 src/build_customer_profiles.py：
- 读取 data/ 下 4 个 json（sample_emails / ambiguous_emails / e2e_emails / inbox_snapshot）
- 按客户名聚合（inbox_snapshot 用清洗后的客户字段，注意复用 mail_connector.clean_body）
- 每个客户输出一篇「档案」，结构：
    {
      "customer": "Van Dijk Bouwgroep B.V.",
      "chunks": [
        {"chunk_id": "VDB-001", "date": "...", "category": "询盘",
         "text": "客户询价 aluminum composite panel (ACP) 8,500 sqm，单价 USD 14.20/sqm，交期 2026-09-18"},
        ...
      ]
    }
- text 要写成**自然语言陈述句**，不是字段罗列——检索靠的是语义，不是字段匹配
- 输出到 data/customer_profiles.json
- 最后告诉我：一共几个客户、多少个 chunk、每个客户几封

先只输出脚本，我确认后再跑。
```

**✅ 完成判定**：`data/customer_profiles.json` 存在，客户数 ≥ 5，chunk 数 ≥ 15

### 5.3 写 BM25 检索器（25 分钟）

**📋 可复制指令**：

```
写 src/retriever.py —— BM25 检索器，要求：

1. 类 BM25Retriever，方法 build(chunks) / search(query, top_k=3)
2. 分词：英文邮件，规则要写清楚——
   - 转小写
   - 按非字母数字切分
   - 过滤长度 <2 的 token
   - 去英文停用词（自己维护一个小的即可，不要装 nltk）
   - **保留数字**（数量、价格、日期是重要检索信号，别丢）
3. 用 rank_bm25 的 BM25Okapi
4. 返回 [(chunk_id, score, text), ...]，score 保留 4 位小数
5. 加一个 --demo 模式：命令行跑 3 个示例 query，打印 top3 结果

依赖 import 路径用 sys.path 兼容我的 pkgs 目录，或运行时设 PYTHONPATH。
写完直接跑 --demo，让我看效果。
```

**运行方式**（记牢，以后都这么跑）：

```bash
cd "C:/Users/Administrator/WorkBuddy/workbuddy学习/projects/外贸邮件Agent"
PYTHONPATH="C:/Users/Administrator/.workbuddy/binaries/python/pkgs" \
  "C:/Users/Administrator/AppData/Local/Programs/Python/Python311/python.exe" src/retriever.py --demo
```

**✅ 完成判定**：3 个示例 query 的 top1 结果**看起来是对的**（不是靠你肉眼硬凑，是真的相关）

> 📝 **面试素材**：BM25 的 k1 / b 两个参数是什么含义、怎么调——  
> 今天跑通后顺手查一下，明天调参时要用。（k1 控词频饱和，b 控文档长度归一化）

### 5.4 为什么「保留数字」这条要单独强调

外贸场景的检索 query 长这样：

- "8,500 sqm ACP 的客户是谁"
- "报过 USD 9.80 的是哪家"

**数字是最强的区分信号**，但它们通常不在停用词表里、也不会被过滤——  
真正的坑是**分词把 `8,500` 切成 `8` 和 `500`**。检查一下你的分词器有没有这个问题。

---

## 六、任务 ④ 建评测集 + baseline（20 分钟）🔴 核心

> **这是今天最值钱的一步**。没有评测数字的 RAG = 玩具。

### 6.1 建 20 条 query + 标注（12 分钟）

**📋 可复制指令**：

```
建检索评测集 data/eval_queries.json，20 条，覆盖这 4 类难度：

| 类型 | 条数 | 示例 |
|---|---|---|
| 客户名直接命中 | 6 | "Van Dijk 之前询过什么产品？" |
| 产品/规格检索 | 5 | "哪些客户问过 LED Downlight？" |
| 数字/金额检索 | 4 | "报过 USD 9.80 的是哪家？" |
| **语义检索（关键词不重合）** | 5 | "有没有客户投诉过质量问题？" |

每条结构：
{"query": "...", "relevant_chunk_ids": ["VDB-001"], "difficulty": "客户名直接命中", "note": "..."}

要求：
- 第 4 类「语义检索」是重点——query 里故意不出现 chunk 中的关键词，
  这批是明天验证「向量检索到底比 BM25 强多少」的关键证据
- relevant_chunk_ids 必须是你实际核对过的，不要瞎标
- 至少要包含 3 条你**预计 BM25 会失败**的（诚实标注，别为了让数字好看）

输出 json 后，用表格告诉我这 20 条的分布。
```

### 6.2 跑评测（8 分钟）

**📋 可复制指令**：

```
写 src/eval_retrieval.py，跑检索评测：

指标：
- Recall@1 / Recall@3 / Recall@5（正确答案是否出现在 top-K 里）
- MRR（平均倒数排名）
- **按 difficulty 分组统计**（这是最重要的一张表）

输出：
- 控制台打印汇总表
- 保存 output/检索评测-baseline.md

另外单独列出「BM25 失败的 query」清单，每条注明失败原因。
```

**✅ 今日核心交付**：一张长这样的表

| 难度类型     | 条数     | Recall@3 | MRR   |
| -------- | ------ | -------- | ----- |
| 客户名直接命中  | 6      | ?        | ?     |
| 产品/规格检索  | 5      | ?        | ?     |
| 数字/金额检索  | 4      | ?        | ?     |
| **语义检索** | 5      | ?        | ?     |
| **合计**   | **20** | **?**    | **?** |

> 🎯 **这张表就是明天对比的基准线**。  
> 明天的产出是"混合检索把语义类从 X% 提到 Y%"——**没有今天这个数字，明天等于白干**。

---

## 七、任务 ⑤ 记忆写入（5 分钟）

**📋 可复制指令**：

```
写入工作区日志 .workbuddy/memory/2026-09-01.md：

1. RAG Day1：客户档案语料结构、chunk 数、BM25 检索器实现要点
2. **baseline 评测数字**（Recall@K / MRR + 按难度分组）—— 供明天对比，别丢
3. BM25 失败的 query 清单和失败原因
4. 定时任务 9:01 首次运行的确认结果
5. Day2 三项欠账的处置结论（降级/砍掉及理由）

同时更新 .workbuddy/memory/MEMORY.md：项目已新增 RAG 检索层（BM25），
embedding 方案已定为智谱 embedding-3（0.5元/百万tokens，OpenAI 兼容）
```

---

## 📦 今日交付清单

- [ ] 定时任务 9:01 首次运行结果已确认（正常/无新邮件/失败）
- [ ] 能口述 RAG 五环节，说清为什么先做 BM25
- [ ] `data/customer_profiles.json` 已生成（客户 ≥5，chunk ≥15）
- [ ] `src/retriever.py` 已跑通，`--demo` 的 top1 结果确实相关
- [ ] 分词器已验证**不会把 `8,500` 切成 `8` + `500`**
- [ ] `data/eval_queries.json` 20 条已建，含 5 条语义检索 + ≥3 条预计 BM25 会挂的
- [ ] **baseline 数字已跑出并保存**（这是今天唯一的硬指标）
- [ ] 记忆已写入

---

## ⚠️ 今日三条红线

1. **今天不许装 LangChain / Chroma / LlamaIndex**。框架是明天之后的事，  
   今天一装，1.5 小时全耗在环境上。
2. **评测集的标注不许注水**。`relevant_chunk_ids` 必须实际核对过，  
   且必须包含预计失败的样本——**含失败样本的评测才有诊断价值**。
3. **别急着接 LLM**。今天只做「检索」，不做「生成」。  
   RAG 的 R 没测准就上 G，出了问题你不知道是检索错了还是生成错了。

---

## 🗓️ 明日预告（9/2 周三 · 1.5h）

**RAG Day 2：接向量检索 + 混合检索对比**

| # | 任务                            | 用时    | 前置             |
| - | ----------------------------- | ----- | -------------- |
| 1 | 申请智谱 Key，跑通 embedding-3       | 20min | 🔴 **需你今晚先注册** |
| 2 | 写向量检索器（余弦相似度，NumPy 即可，暂不上向量库） | 30min | 无              |
| 3 | 混合检索（BM25 + 向量，RRF 融合）        | 25min | 今天 baseline    |
| 4 | 对比评测 → **产出"混合检索提升 X%"**      | 15min | 无              |

> 明天结束时你会拿到：**"纯 BM25 语义类 Recall@3 是 X%，混合检索提到 Y%，提升 Z 个百分点"**  
> —— 这句话就是 JD 原话级别的答案。

**🔴 今晚可做的 5 分钟前置**：注册 <https://open.bigmodel.cn> 拿 API Key，  
填进 `config/llm_config.json` 的 `zhipu.api_key`（已有 .gitignore，不会泄露）。

---

## 💬 卡壳处理

| 问题              | 怎么办                                  |
| --------------- | ------------------------------------ |
| rank_bm25 装不上   | 自己写，BM25 公式就 30 行，写完反而面试能讲原理         |
| 客户名聚不齐（同一家写法不同） | 先跑，记录差异，这是**真实工程问题**，周末专门做归一化        |
| 评测集标不准          | 减到 10 条也要标准，**宁可少不可假**               |
| 时间不够            | 保 ④ 砍 ③ —— **baseline 数字 > 检索器实现细节** |
| 卡住超 10 分钟       | 记下来跳过，继续下一项                          |

---

> 🎯 **今日一句话目标**：拿到 **20 条 query 的 BM25 baseline 数字**。  
> 检索器写得漂不漂亮不重要，**这个数字是你明天所有对比的地基**。
