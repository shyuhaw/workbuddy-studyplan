# Day7 学习计划 · 2026-09-05（周六）

## 一句话结论

**把Agent的核心发现反向移植回Pipeline**：用流水线的成本，拿Agent的效果。Pipeline query优化是今天最高性价比的任务。

---

## 进度背景

| 已完成 | 今日任务 |
|---|---|
| ①分类 ✅ ②提取 ✅ ③决策 ✅ | **④Pipeline query优化 + A/B对比重跑** |
| ④RAG检索 ✅ ⑤RAG生成 ✅ | ⑥Agent引擎 ✅（已做，max_rounds=10表现最佳） |
| ⑦飞书案例 ✅ | ⑧MCP服务封装（可延后） |

**关键数据**：
- max_rounds=10：Agent可用率95%，成本¥0.0332/条
- max_rounds=6：Agent可用率70%，降级15次（严重）
- Pipeline r6：可用率55%（已有优化基础）
- Pipeline r10：可用率20%（query构造需优化）

**核心洞察**：Pipeline与Agent差距的主因不是模型能力，而是**query构造质量**。

---

## 必做任务（2项）

### 任务① Pipeline Query优化落地（60min）

**目标**：修改workflow.py的query构造逻辑，从简单拼接升级为带意图词的丰富表达。

**具体步骤**：

1. **复制意图词表**（5min）
   - 从 `src/eval_query_optimization.py` 复制 `INTENT_TERMS` 列表到 `src/workflow.py`
   - 定义 `QUERY_INTENT_TERMS = ["quotation", "price", "cost", ...]`

2. **修改query构造函数**（25min）
   - 找到 `workflow.py::_build_query()` 或等效函数
   - 从 `f"{customer} {product}"` 升级为：
     ```python
     query = f"{customer} {product}"
     if subject:
         query += f" {subject}"
     # 从邮件正文提取意图词
     intent_words = [w for w in QUERY_INTENT_TERMS if w in body.lower()]
     if intent_words:
         query += " " + " ".join(intent_words)
     ```
   - 保持向后兼容：原逻辑作为默认，新逻辑作为可选参数

3. **单元测试验证**（15min）
   - 构造3-5个测试邮件，手动验证query输出
   - 对比优化前后的query长度和内容差异

4. **回归测试**（15min）
   - 跑现有demo_workflow.py确认无破坏
   - 确认demo_server.py仍可正常运行

---

### 任务② 统一口径A/B对比重跑（60min）

**目标**：在相同条件下对比Pipeline（优化后）vs Agent，得出公平结论。

**具体步骤**：

1. **修改eval脚本**（15min）
   - 在 `src/eval_agent.py` 中新增参数 `pipeline_query_mode`
   - 可选值：`"simple"`（原逻辑）/ `"optimized"`（新逻辑）
   - 当 `pipeline_query_mode="optimized"` 时使用优化后的query构造

2. **跑A/B对比实验**（30min）
   ```bash
   python src/eval_agent.py --max-rounds 10 --pipeline-query-mode optimized
   ```
   - Pipeline侧：使用优化后query，20条样本
   - Agent侧：保持原有逻辑，20条样本
   - 记录有用率、成本、耗时、降级次数

3. **分析结果**（15min）
   - 对比优化前后Pipeline可用率变化
   - 对比Pipeline优化后 vs Agent表现差距
   - 计算性价比比（可用率/成本）

4. **保存结果**（10min）
   - 输出 `output/eval_final_comparison.json`
   - 写入执行摘要（文本格式，方便README引用）

---

## 可选任务（2项）

### 任务③ README更新（30min）

**更新内容**：
- 新增「Pipeline Query优化」一节
- 更新效果数据表格（加入优化后Pipeline数据）
- 更新「技术亮点」部分，补充「query自优化能力」的描述
- 更新架构图，体现Pipeline和Agent的对比关系

**时间**：30min

---

### 任务④ dist更新部署（30min）

**更新内容**：
- 更新 `dist/index.html` 效果数据
- 更新「项目一：跨境客户邮件智能处理 Agent」章节
- 重新部署到 workbuddy.link

**注意**：如果今天不部署，可以周日再做。

**时间**：30min（如执行）

---

## 预期成果

| 指标 | 优化前（Pipeline） | 优化后（Pipeline） | Agent（参考） |
|---|---|---|---|
| 有用率 | ~20-55% | 目标 70%+ | 95% |
| 成本/条 | ¥0.0024 | 预计 ¥0.003 | ¥0.0332 |
| 性价比 | 高 | **更高** | 低 |

**核心结论**：
- Pipeline优化后可用率应接近Agent的70%（r6数据）
- 但成本仅为Agent的1/10
- 证明「query优化是关键突破口」这一假设成立

---

## 执行红线

1. **不装新依赖**：所有修改基于现有标准库
2. **成本控制在¥1.0以内**：20条样本，每条约¥0.05上限
3. **不改动评测集**：`data/e2e_emails.json` 保持不变
4. **不掩盖问题**：优化后仍失败的就失败，如实记录
5. **保留原始数据**：optimization前后的eval结果都保留，便于对比

---

## 时间安排

| 时段 | 任务 | 预计耗时 |
|---|---|---|
| 09:00-10:00 | 任务① Pipeline query优化落地 | 60min |
| 10:00-11:00 | 任务② 对比重跑 + 分析 | 60min |
| 11:00-11:30 | 任务③ README更新 | 30min |
| 11:30-12:00 | Git提交 + 总结 | 30min |

---

## 下一步方向（周日/周一）

如果今日任务顺利完成：
1. **Pipeline集成测试**：把优化后的query逻辑集成到完整workflow中
2. **多场景测试**：用真实邮件（非测试集）验证效果
3. **启动MCP服务封装**：把核心能力封装为可复用的MCP工具

如果今日遇到阻塞：
- DeepSeek余额不足 → 先做检索层验证（零成本），等充值后再做生成层对比
- query优化效果不明显 → 分析原因，调整策略
