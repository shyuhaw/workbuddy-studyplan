# Day 5 学习计划 · 2026-09-03（周四 · 工作日）

> **今日主题**：**RAG Day 4（重排）→ 改道「生成闭环」—— 把 R 变成真正的 RAG**
> **时间预算**：下班后 **1.5 小时**
> **今日产出**：`src/context_builder.py` + `src/generator.py` + `src/eval_generation.py` + **忠实度 / 幻觉率数字**
> **本计划只有 4 项任务、2 项必做** —— Day4 的教训，见下

---

## 〇、结论先行（60 秒看完）

### 1. Day4 对账：一笔没动（诚实版，不粉饰）

| Day4 计划项 | 实际 | 判定 |
| --- | --- | --- |
| 补 P@1 / MRR 指标 | ❌ `eval_retrieval.py` 里搜不到 | 未做 |
| `src/reranker.py`（特征 + LLM） | ❌ 文件不存在 | 未做 |
| `src/eval_rerank.py` 三方对比 | ❌ 文件不存在 | 未做 |
| 接入 workflow + README + 记忆 | ❌ | 未做 |

**硬证据**：全空间在 `2026-09-02 10:00` 之后**零文件改动**；`find . -iname "*rerank*"` 返回空。

**归因（不是你的问题，是计划的问题）**：Day4 往 1.5h 里塞了 **5 项任务**，其中「写两个 Reranker + 三方评测」是**从零开新模块**。
前 4 天能超额 3-4 倍，是因为那些任务是「顺着昨天的代码往下写」；**开新模块的成本结构和它完全不同**，同样 1.5h 装不下。

> **今天的修正**：只排 4 项，只 2 项必做，其余明确标注「时间不够直接砍」。

### 2. 但今天不顺延 Rerank —— 因为我扫代码发现了更致命的问题

**`workflow.py` 的「起草回复」是假的。**

- `_step_draft()` → `_render_draft()` —— 看进去是**纯 f-string 模板拼接**
- 全项目 `grep -rn "draft\|起草\|reply" src/agent.py` → **空**
- 也就是说：README 第 190 行写的「起草回复（调用真实智谱向量 + DeepSeek）」，**目前没有调用 DeepSeek，检索到的历史只是被塞进了字符串**

**你现在的真实位置**：

```
文档解析 ✅ → 切分 ✅ → Embedding ✅ → 混合检索 ✅ recall@3=100%
                                                    ↓
              上下文组装 🟡 → 生成 ❌(模板) → 评测 ❌
```

**面试官必问的一句**：「检索出来之后，你怎么用的？」
——现在的答案是「拼进字符串模板」。这一句就能把你四天的检索优化全部归零。

### 3. 今日主轴（结论）

**先补完整性，再做优化。把 R 变成真正的 RAG。**

| 优先级 | 任务 | 理由 |
| --- | --- | --- |
| 🔴 **必做** | ① 上下文组装（去重 / 截断 / **引用编号**） | 生成的前置。没有 `[1][2]` 引用编号，忠实度**无从验证** |
| 🔴 **必做** | ② 接真生成 + ③ 忠实度 / 幻觉评测 | JD 高频句「能处理**幻觉**、延迟、成本」，你现在**零证据**；JD 第 7 条「效果评测」目前也只有检索侧 |
| ⚪ **顺延周六** | Rerank 精排 | 它是**优化项**，不是**完整性缺口**。recall@3 已 100%，晚 3 天不影响故事成立 |
| ⚪ 不做 | 装 ragas / deepeval | 重依赖，本机 venv 创建失败必翻车（Day3 已验证的红线） |

> **排序逻辑一句话**：Rerank 是给闭环**加分**，生成端缺失是闭环**不成立**。先有闭环。

---

## 一、8 分钟认知：RAG 的「G」到底拆成几件事

只要记住这张表，**面试官问"你的 RAG 怎么生成的"就照这个答**：

| 环节 | 干什么 | 你现在的坑 |
| --- | --- | --- |
| **上下文组装** | 把检索到的 chunk 拼进 prompt：去重、截断、**加来源编号** | 🟡 现在是裸塞，无编号、无去重、无截断 |
| **生成** | LLM 基于上下文作答，**必须标注引用** | ❌ 现在是 f-string 模板 |
| **评测** | 忠实度（是否忠于上下文）、答案相关性、幻觉率 | ❌ 完全没有 |

**为什么「引用编号」是这一步的命门**：

> 忠实度评测的本质 = 「答案里的每一句话，能不能在被引用的 chunk 里找到出处」。
> **没有引用编号，你就不知道该去哪条 chunk 里核对** —— 评测直接无法进行。
> 所以 ① 不是"锦上添花的工程细节"，是 ③ 的**硬前置**。

**幻觉的两种，今天只治第一种**：

| 类型 | 说明 | 今天治不治 |
| --- | --- | --- |
| **上下文内幻觉**（不忠实） | 答案里出现上下文**没有**的数字/客户名 —— 模型自己编的 | ✅ **今天治** |
| 上下文外幻觉（知识性错误） | 上下文有，但和客观事实不符 | ❌ 需要外部知识库，超出范围 |

> 📝 **面试一句话**：「幻觉分两种，我优先治**上下文内幻觉**——因为它可测量、可归因、可通过约束生成解决；知识性幻觉要靠外部校验，是另一个问题。工程上先解决能量化的那个。」

---

## 二、时间块（下班后 1.5h）

| 时段 | 时长 | 任务 | 必做 |
| --- | --- | --- | --- |
| 20:00-20:08 | 8min | 认知（上面那张表） | — |
| 20:08-20:30 | 22min | ① 上下文组装：去重 / 截断 / **引用编号** | 🔴 |
| 20:30-21:05 | 35min | ② 接真生成：DeepSeek 生成**带引用**的答案，替换模板 | 🔴 |
| 21:05-21:22 | 17min | ③ 忠实度 + 幻觉评测，跑出数字 | 🔴 |
| 21:22-21:30 | 8min | ④ 落地 + 记忆 | 🟡 |

**时间不够的砍法（按顺序砍）**：先砍 ④（明早补）→ 再砍 ③ 的 LLM-as-judge（只留规则版）→ **①② 不许砍**。

---

## 三、任务 ①：上下文组装（22 分钟）🔴

**目标**：`top_k` 命中 → 去重 → 截断 → 编号 → 可追溯的 prompt 片段。

**📋 可复制指令**：

```
新建 src/context_builder.py，实现 build_context(query, hits, max_chars=2000)：

输入：hits = HybridRetriever.search(query, top_k=5, return_scores=True)
      每条是 {"id","customer","text","score"}

处理顺序（顺序不能乱）：
1. 去重：按 chunk id 去重；再按 text 前 60 字去重（防同义重复）
2. 截断：从 top-1 开始累加，总字符数超过 max_chars 就停，
   绝不截断单条 chunk 的中间（要么整条要，要么整条丢）
3. 编号：按顺序标 [1] [2] [3]...，返回 (context_str, id_map)
   id_map = {"1": "C07", "2": "C12"}  ← 这个 map 是后面验证引用的钥匙

输出格式（每条一行，customer 和 id 都要带，用于溯源）：
  [1] (C07 | Global Import Ltd.) LED Panel Light PL-6060 40W，2025-03 成交价 USD 12.80/只...
  [2] (C12 | Bright Trading) ...

要求：
1. 零依赖，纯标准库
2. 加 --demo：跑 3 条 query，打印组装前后的对比（看截断是否真的生效）
3. 空 hits 返回 ("（无相关历史记录）", {})，不要让下游崩
```

**✅ 完成判定**：跑 `--demo` 能看到带 `[1] (C07 | 客户名)` 编号的上下文，且 `id_map` 打印正确。

> ⚠️ **坑预警**：`max_chars` 别设太大。语料才 21 条、单条很短，2000 字符基本不会触发截断。
> 那就在 demo 里手动把 `max_chars=200` 跑一次，**证明截断逻辑真的生效**——
> 「写了但没验证过」在面试里等于没写。

---

## 四、任务 ②：接真生成（35 分钟）🔴

**目标**：把 `_render_draft()` 的模板换成真的 DeepSeek 调用，**答案必须带引用编号**。

### 4.1 新建 `src/generator.py`

**📋 可复制指令**：

```
新建 src/generator.py，实现 generate_answer(query, context_str, id_map)：

1. 复用已有封装，不要重写 HTTP 调用：
   from llm_fallback import DeepSeekProvider
   provider = DeepSeekProvider(api_key)   # key 从 config/llm_config.json 读，沿用现有读取逻辑

2. System prompt（核心在最后三条约束）：
   你是外贸业务助手，只能依据给定的「历史往来片段」回答。
   规则：
   - 每句话末尾必须标注依据的片段编号，格式 [1] 或 [1][2]
   - 只用片段中出现过的数字、客户名、产品名，禁止推测
   - 片段中没有的信息，直接回答「依据现有记录无法确认」，不要编造
   - 不要输出片段编号之外的内容

3. User prompt：
   历史往来片段：
   {context_str}

   问题：{query}

   请依据上述片段回答（标注引用编号）：

4. 返回结构化结果：
   {"answer": "...", "cited_ids": ["C07","C12"], "raw": "..."}
   cited_ids 从答案里正则抽取所有 [n]，再通过 id_map 映射回真实 chunk id

5. 优雅降级（必须写，这是你项目的一贯风格）：
   调用失败 / 超时 5s / 答案里一个编号都没有 →
   回退到现有 _render_draft() 模板，返回时打上 "mode": "template_fallback"
6. 加 --demo：跑 3 条 query，打印「问题 / 上下文 / 带引用答案 / 回退标记」
```

### 4.2 接入 `workflow.py`（改动要小）

**📋 可复制指令**：

```
改 src/workflow.py 的 _step_draft()：

- 把现有的 _render_draft() 保留，改名为 _render_draft_template()
  （它就是降级路径，不要删——删了就没有 fallback 了）
- _step_draft() 改为：
    1. 用 build_context() 组装上下文
    2. 调 generate_answer()
    3. 失败或 mode == "template_fallback" → 用 _render_draft_template()
    4. 把 generator 返回的 cited_ids 存进 self.cited_ids
      （后面 summary() 里能打印出来，构成「答案 → 来源 chunk」的审计链）
- 要求：WorkflowCase 构造签名不变，demo_workflow.py 零改动即可跑
```

**✅ 完成判定**：`python src/demo_workflow.py` 正常跑完，草稿里出现 `[1]` 这种引用标记。

> 🎯 **这一步做完，你的 RAG 才第一次真正闭合。**
> 明天开始，「检索到了然后呢」这个问题你有答案了。

---

## 五、任务 ③：忠实度 + 幻觉评测（17 分钟）🔴

**这是今天的硬指标。** 没有数字，前两步等于没做。

### 5.1 评测方法（两层，先规则后 LLM）

| 层 | 指标 | 怎么算 | 成本 |
| --- | --- | --- | --- |
| **规则层**（先跑） | **数字可溯源率** | 答案里每个数字/金额/数量，能否在 `cited_ids` 对应的 chunk 原文里找到 | ¥0 |
| **规则层** | **实体可溯源率** | 答案里的客户名 / 产品型号，同上 | ¥0 |
| **规则层** | **引用准确率** | 答案标的 `[n]` 是否都在 id_map 里（防模型编造 [7]） | ¥0 |
| **LLM 层**（后跑） | **忠实度** | 把答案拆成若干条"声明"，逐条让 DeepSeek 判 `supported / not_supported` | ≈分厘级 |

**📋 可复制指令**：

```
新建 src/eval_generation.py，对 20 条评测 query 跑端到端生成评测：

流程（每条 query）：
  检索(hybrid, top_k=5) → build_context → generate_answer → 三层打分

指标输出（分「整体 20 条 / 仅语义类 5 条」两组）：
  1. 数字可溯源率 = 可溯源的数字个数 / 答案中数字总个数
  2. 实体可溯源率（客户名 + 产品型号）
  3. 引用准确率 = 合法引用数 / 总引用数
  4. 忠实度（LLM-as-judge，可选层）= supported 声明数 / 总声明数
  5. 生成耗时均值 + 总 token + 折算成本
  6. 模板回退次数（降级被触发了几条）

落盘 output/eval_generation.json，并逐条列出「检出幻觉」的 query：
  打印 query / 答案 / 那个找不到的数字 / 它本该来自哪个 chunk

命令行：
  python src/eval_generation.py                 # 规则层 + LLM 层
  python src/eval_generation.py --rules-only    # 只跑规则层（时间不够用这个）
```

**✅ 今日核心交付**：一张长这样的表

| 指标 | 数值 |
| --- | --- |
| 数字可溯源率 | ?% |
| 实体可溯源率 | ?% |
| 引用准确率 | ?% |
| 忠实度（LLM judge） | ?% |
| 幻觉率（= 1 − 可溯源率，取较差那项） | **?%** |
| 单次生成耗时 / 成本 | ?s / ¥? |

> 🎯 **今天成功 = 忠实度 ≥ 85% 且拿到了幻觉率具体数字。**
> 达不到**不许改评测集、不许删失败样本**——诚实的负结果比好看的数字值钱得多，
> 而且「我测出幻觉率 X%，然后用了 Y 方法压到 Z%」本身就是最好的面试故事。

### 5.2 跑完立刻填进作品集的这句话

```
「RAG 全链路闭合：混合检索 recall@3 = 100%（BM25 + 智谱向量 + 意图自适应，
 生成端强制引用标注，实测数字可溯源率 X%、实体可溯源率 Y%、忠实度 Z%，
 单次生成 ¥N。幻觉未靠拍脑袋解决，而是靠「引用可验证」把它变成可测量的指标。」
```

**这句话一口气命中 JD 四处**：混合检索 ✅、重排序（周六补）、上下文组装 ✅、「能处理幻觉」✅、效果评测 ✅。

---

## 六、任务 ④：落地 + 记忆（8 分钟）🟡

**📋 可复制指令**：

```
1. README 第十三节后面新增「生成端（Day05）」小节：
   - 诚实写清：此前 _step_draft 是模板拼接，今天替换为 LLM 生成
   - 贴上面那张评测表（数字按实测填，不许预估）
   - 目录树加 src/context_builder.py / src/generator.py / src/eval_generation.py

2. 写工作区日志 .workbuddy/memory/2026-09-03.md：
   - 评测表完整数字
   - 检出幻觉的 query 清单（含归因：是检索错了，还是生成编的）
   - 「检索错 vs 生成编」这个归因很重要——两者的解法完全不同
   - 模板回退被触发了几次

3. 更新 .workbuddy/memory/MEMORY.md：RAG 链路 G 端已闭合（R→G 完整）

4. README footer 日期 9/01 → 9/03
```

> 📌 作品集.html / dist 的更新**今天不做**（8 分钟装不下，且要重新部署会让公网链接变）。
> 列入周六清单，一次性更新 + 重新部署。

---

## 📦 今日交付清单

- [ ] 能口述「上下文组装 / 生成 / 评测」三段的职责，说清**为什么引用编号是评测的硬前置**
- [ ] `src/context_builder.py` 已实现（去重 / 截断 / 引用编号 + id_map），`--demo` 验证过截断生效
- [ ] `src/generator.py` 已实现，**答案带引用编号**，失败自动回退模板不崩
- [ ] `workflow.py` 已接入，`demo_workflow.py` 零改动可跑
- [ ] `src/eval_generation.py` 已跑出**忠实度 / 可溯源率 / 幻觉率**数字（**今天硬指标**）
- [ ] README 已更新 + 记忆已写入

---

## ⚠️ 今日三条红线

1. **不许装 ragas / deepeval / sentence-transformers。**
   这些包会拖进 torch、numpy 一整条依赖链。本机 venv 创建失败、`--target` 装重依赖极易翻车
   （Day3 的 `greenlet._greenlet` 缺失就是这么来的）。
   **今天全部自己实现：规则层纯正则，LLM 层直接用你已有的 `DeepSeekProvider`。**
   而且自己实现反而更好——**你能讲清每个指标怎么算的，调包讲不清**。

2. **20 条评测集不许动。** 它是 Day3 钉死的资产，动了就跟前三天不可比，
   「BM25 → 混合 → 生成闭环」的演进故事直接断掉。

3. **不许为了忠实度好看而缩短答案。**
   「只回答依据现有记录无法确认」确实能把幻觉率刷到 0，但那是**无用的系统**——
   如果检出幻觉率高，正确做法是记下来归因（检索错 vs 生成编），那是明天的优化方向。

---

## 🗓️ 明日预告（9/4 周五 · 1.5h）

**按今天检出的幻觉归因走，二选一：**

| 今天的归因 | 明天的路 |
|---|---|
| **检索错了**（retrieved 里压根没有正确 chunk） | 做 **Rerank 精排**（Day4 原计划，顺延到这天），顺带把 P@1 / MRR 指标补上 |
| **生成编的**（chunk 里有，但答案写错） | 做**生成端约束**：low temperature + 结构化输出 + 拒答阈值 + 二次校验 |

> 明天开工前**先看一眼今天的归因清单**，再决定走哪条路——别默认还做 Rerank。

---

## 📌 欠账清单（本周六 · 8-12h）

| 欠账 | 来源 | 预估 | 优先级 |
| --- | --- | --- | --- |
| **Rerank 精排（Day4 原计划）** | 9/2 未执行 | 1.5h | 🔴 周六第一件事 |
| P@1 / MRR 指标补齐 | 9/2 未执行 | 15min | 🔴 和 Rerank 一起做 |
| 作品集.html + dist 更新 + 重新部署 | 今天顺延 | 30min | 🟡 |
| 飞书案例 5 项待补数据（问同事） | 9/1 遗留 | 10min | 🟡 |
| 简历改造（用业务效果量化重写） | L4 | 1.5h | 🟡 越早越好，直接影响投递 |

> **周六排法建议**：Rerank + P@1/MRR（1.75h）→ 简历改造（1.5h）→ 作品集更新部署（0.5h）→ 飞书数据（10min）。
> 剩下时间自由支配。**别再一天塞 5 项。**

---

## 💬 卡壳处理

| 问题 | 怎么办 |
| --- | --- |
| DeepSeek 调用失败 / 余额不足 | 降级路径**本来就要求写**，直接触发模板回退，顺手就测了 |
| 答案里一个引用编号都没有 | 多半是 prompt 里编号格式没说清，改成「每句话**末尾**必须带 [n]」并给 1 个 few-shot 示例 |
| 忠实度数字很难看（<60%） | **先归因**：把失败样本分成「检索错」和「生成编」两堆。分不出来就先记下，明天处理 |
| 时间不够 | 保 ① ②，砍 ③ 的 LLM 层只留规则层，砍 ④ 明早补 |
| 卡住超 10 分钟 | 记下来跳过，继续下一项 |

---

## 🔁 给这个自动化任务的一句校准

> Day4 计划 5 项、实际 0 项。
> **从今天起：每天最多 4 项，必做项 ≤ 2，其余全部标注「可砍」。**
> 计划的价值在于被执行，不在于写得完整。

---

> 🎯 **今日一句话目标**：拿到**幻觉率 / 可溯源率的具体数字**。
> 写不写得出漂亮代码不重要——**「RAG 的 G 我做过，而且我测过它有没有编」**，
> 这句话才是 JD 高频句「能处理幻觉」的唯一硬证据。
