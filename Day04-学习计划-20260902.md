# Day 4 学习计划 · 2026-09-02（周三 · 工作日）

> **今日主题**：**RAG Day 3 —— Rerank 精排，把 P@1 从 75% 拉到 90%+**
> **时间预算**：下班后 **1.5 小时**
> **今日产出**：`src/reranker.py` + 三方对比评测表 + **P@1 / MRR 精排数字**

---

## 〇、结论先行（30 秒看完）

### Day3 对账：计划全做完了，还超了两天的量

| Day3 计划项 | 实际 | 判定 |
| --- | --- | --- |
| 确认定时任务出活 | ✅ 发现被 PAUSED，已修复 | 需持续观察 |
| RAG 五环节骨架认知 | ✅ | 超额 |
| 建语料 + BM25 检索器 | ✅ 21 chunk + **自写 BM25，零依赖** | 超额（没用 rank_bm25） |
| 评测集 + baseline 数字 | ✅ recall@1=85% / @3=90% / @5=90%，语义类 40% | 达标 |
| 记忆写入 | ✅ | 达标 |

**你实际做的是 4 天的量**：BM25 → 混合检索 → 意图自适应调权（**recall@3=100%**）→ 工作流状态机 → 业务效果实测 → 飞书第二案例（CLI 全量审计）→ 公网 Web Demo。

**结论：RAG 的「R」你已经打穿了。今天开始打「排得准不准」。**

### 今天的判断

| 优先级 | 任务 | 理由 |
| --- | --- | --- |
| 🔴 主轴 | **Rerank 精排** | JD 第 1 条「RAG 全链路」原文含 **重排序**，你是唯一空白项 |
| 🟡 顺手 | 补 P@1 / MRR 指标 | 现有评测只测 recall@k，量不出「排第几」 |
| ⚪ 不做 | 装 BGE-Reranker / torch | 重依赖，这台机器 venv 创建失败，装了今晚就没了（见红线） |

---

## 一、为什么今天是 Rerank

### 1.1 你现在的位置（对照 JD 第 1 条原文）

> JD 原文：RAG 全链路 = 「文档解析切分、**Embedding**、向量库、混合检索(向量+BM25)、**重排序**、上下文组装」

| 环节 | 状态 | 证据 |
| --- | --- | --- |
| 文档解析 | ✅ | `clean_body()` 处理真实脏邮件 |
| 切分 | ✅ | 一封往来 = 一个 chunk（天然切分） |
| Embedding | ✅ | 智谱 embedding-3，真实调用 |
| 向量库 | ⚪ 内存余弦 | 21 条语料用不上，暂不做 |
| 混合检索 | ✅ | BM25 + 向量 RRF，**recall@3=100%** |
| **重排序 Rerank** | ❌ **今天补** | — |
| 上下文组装 | 🟡 部分 | workflow 里塞了 top3，无去重/引用标注 |

### 1.2 你现在的真实痛点，正好是 Rerank 的靶心

| 指标 | 当前值 | 说明 |
| --- | --- | --- |
| recall@3 | **100%** | 正确答案**都在**前 3 里 |
| recall@1 | **75%** | 但**只有 75% 排在第一** |

**这 25% 的差距就是 Rerank 的全部价值。**

一句话讲给面试官：
> 「两阶段检索：混合召回负责**不漏**（recall@3=100%），Rerank 精排负责**排准**（P@1 从 75% 提到 X%）。召回和精排的目标不同，所以要用不同的方法——这也是为什么不能直接靠调融合权重解决问题。」

> 💡 这正是你昨天踩过的坑：**RRF 全局权重是钝刀**（w_vec=3.0 修语义类却崩掉数字类）。
> 昨天的解法是「意图自适应路由」，今天的解法是「换一把更精细的刀」——两件事一脉相承。

---

## 二、时间块（下班后 1.5h）

| 时段 | 时长 | 任务 | 产出 |
| --- | --- | --- | --- |
| 20:00-20:12 | 12min | ① 两阶段检索认知：召回 vs 精排 | 能口述链路 |
| 20:12-20:25 | 13min | ② 给评测加 **P@1 / MRR** 指标，跑出精排基线 🔴 | 基线数字 |
| 20:25-20:55 | 30min | ③ 实现两种 Reranker 🔴 | `src/reranker.py` |
| 20:55-21:18 | 23min | ④ 三方对比评测 + 延迟/成本 🔴 | 对比表 |
| 21:18-21:30 | 12min | ⑤ 择优接入 workflow + README/作品集 + 记忆 | 落地 |

---

## 三、任务 ① 两阶段检索认知（12 分钟）

只要记住这张表：

| | 召回（Retrieval） | 精排（Rerank） |
| --- | --- | --- |
| 目标 | **不漏**（高 recall） | **排准**（高 precision@1） |
| 输入 | 全库 21 条 | 召回的 top-10 |
| 方法 | BM25 + 向量，可并行、极快 | 逐条精算，慢但准 |
| 特点 | 双塔（query 和文档各自独立编码，**从不交互**） | 交叉编码（query 和文档**一起看**） |
| 你的现状 | ✅ recall@3=100% | ❌ 今天做 |

**为什么双塔必然漏掉语义**：query 和文档在编码时没见过面，只能靠向量空间里的距离猜。
**Rerank 让它们见上一面**，所以能抓住"投诉质量问题" ↔ "arrived damaged, claim USD 2,400" 这种零词重合的关联。

> 📝 **面试一句话**：「召回要快，所以用双塔；精排要准，所以用交叉编码。这是工程上的必然分工，不是谁替代谁。」

---

## 四、任务 ② 补 P@1 / MRR 指标，跑精排基线（13 分钟）🔴

现有 `eval_retrieval.py` **只统计 recall@k**，量不出「正确答案排第几」——今天要优化的就是这个，得先有尺子。

**📋 可复制指令**：

```
改 src/eval_retrieval.py，在现有 recall@k 之外新增两个排序质量指标：

1. P@1（Precision@1）：top1 命中的 query 占比
2. MRR（Mean Reciprocal Rank）：每条 query 取「第一个正确答案的排名倒数」，再求平均
   - top1 命中 = 1.0，rank2 命中 = 0.5，rank3 = 0.333，未命中 = 0

要求：
- 按「全部 20 条 / 仅语义类」两组分别输出（沿用现有分组逻辑）
- search() 调用改成 top_k=10（给 rerank 留候选池，不能只用 top5）
- 控制台打印格式与现有表格保持一致
- 落盘 output/eval_rerank_baseline.json（不要覆盖 eval_retrieval.json，那是 Day3 的资产）

跑一次 --mode hybrid（意图自适应开启），把这三行数字抄给我：
  精排基线： P@1 = ?  MRR = ?  recall@3 = ?（应为 100%）
```

**✅ 完成判定**：拿到 `P@1 ≈ 75%`、`MRR ≈ ?`、`recall@3 = 100%` 三个数

> 🎯 这三个数就是今天所有对比的地基。**没跑出基线不许往下做**。

---

## 五、任务 ③ 实现两种 Reranker（30 分钟）🔴

### 5.1 为什么做两种（不是过度设计）

| 方案 | 成本 | 延迟 | 精度 | 定位 |
| --- | --- | --- | --- | --- |
| **A. 特征精排** | 0 | ~0ms | 中 | 兜底 + 对照 |
| **B. LLM 精排** | ≈分厘级 | 1-2s/次 | 高 | 主力 |

两个都要——**A 是 B 挂了之后的降级路径**（和你项目一贯的优雅降级风格一致），
而且没有 A 做对照，你证明不了 B 花的钱值不值。

### 5.2 方案 A：特征精排（12 分钟）

**核心思路**：把「排第一应该长什么样」写成可解释的加权特征。

**📋 可复制指令**：

```
在 src/reranker.py 里写 FeatureReranker：

对召回的 top-10 候选，逐条算这 5 个特征，加权求和后重排：

| 特征 | 含义 | 建议权重 |
|---|---|---|
| f_bm25   | BM25 原始分（归一化到 0-1）        | 1.0 |
| f_vec    | 向量余弦相似度（归一化到 0-1）     | 1.0 |
| f_name   | 客户名精确命中（query 里出现 chunk 的客户名）| +1.5 |
| f_num    | 关键数字精确命中（金额/数量/型号，用你已有的 is_number_query 逻辑）| +1.5 |
| f_recency| 时间新鲜度（近 90 天 chunk 加分）   | +0.3 |

要求：
1. 权重全部写成模块级常量，方便调
2. 加 explain(query, top_k) 方法，打印每条候选的 5 个特征分和总分——
   「可解释」是你能拿去面试讲的东西，别省
3. 归一化用 min-max，避免 BM25 和余弦量纲打架
4. 接口与 HybridRetriever 对齐：rerank(query, candidates, top_k) -> [(id, score, text), ...]
```

### 5.3 方案 B：LLM 精排（listwise，18 分钟）

**为什么是 listwise 而不是 pairwise**：pairwise 要 C(n,2) 次调用，10 个候选 = 45 次；listwise 一次搞定。

**📋 可复制指令**：

```
在 src/reranker.py 里加 LLMReranker：

1. 复用你已有的 DeepSeek 调用封装（llm_fallback.py / llm_extract.py 里的 call 函数），
   不要再写一遍 HTTP 调用
2. Prompt 用 listwise 格式（一次性给 10 个候选，让模型输出排序）：

   你是外贸邮件检索的排序助手。根据用户问题，对下列候选片段按相关度从高到低排序。
   只输出编号序列，用逗号分隔，不要解释。

   用户问题：{query}

   候选片段：
   [1] {chunk_1_text}
   [2] {chunk_2_text}
   ...

   排序结果：

3. 解析要健壮：模型可能输出 "3,1,2" / "[3, 1, 2]" / "3、1、2" 甚至带废话——
   正则提取所有数字，去重，缺失的候选按原顺序补在末尾
4. **必须优雅降级**：调用失败 / 超时(3s) / 解析出的编号数 < 候选数一半 →
   直接返回原顺序，打印告警，不抛异常
5. 加 --demo 模式跑 3 条 query，打印重排前后的顺序变化
```

**⚠️ 关键约束：只对「非纯数字类」query 启用 LLM 精排**

沿用昨天验证过的**意图自适应路由**思路：

| query 类型 | 走哪条路 | 理由 |
| --- | --- | --- |
| 数字/金额类（USD 9.80、8,500 sqm） | **跳过 LLM，直接用 A 或原顺序** | BM25 对精确数字比 LLM 更可靠，昨天已验证 |
| 语义/混合类 | **LLM 精排** | 这才是 LLM 的强项 |

> 这一步是今天最值钱的**工程判断**，不是技术细节。
> 「不是所有 query 都值得花钱 rerank」——这句话面试官爱听。

---

## 六、任务 ④ 三方对比评测（23 分钟）🔴

**📋 可复制指令**：

```
写 src/eval_rerank.py，跑三方对比。每个方案都对同样的 20 条 query 评测：

| 方案 | 说明 |
|---|---|
| ① baseline | 现网最优：hybrid + 意图自适应（无精排） |
| ② +特征精排 | FeatureReranker |
| ③ +LLM 精排 | LLMReranker（数字类 query 自动跳过） |

指标：P@1 / MRR / recall@3 / recall@5
另外统计：20 条 query 的总耗时、LLM 调用次数、token 消耗与折算成本

输出：
1. 控制台打印对比表（用下面的表头）
2. 落盘 output/eval_rerank.json
3. 单独列出「被 LLM 精排救回来」和「被 LLM 精排搞砸」的 query 各有哪些，逐条说明原因
```

**✅ 今日核心交付**：一张长这样的表

| 方案 | P@1 | MRR | recall@3 | 总耗时 | 成本 |
| --- | --- | --- | --- | --- | --- |
| ① baseline（无精排） | 75% | ? | 100% | ~0s | ¥0 |
| ② +特征精排 | ? | ? | ? | ~0s | ¥0 |
| ③ +LLM 精排（路由） | **?** | **?** | ? | ?s | ¥? |

> 🎯 **P@1 从 75% → 90%+ 且不丢 recall@3 = 今天成功。**
> 达不到也没关系：**诚实的负结果 = 明天继续优化的起点**，不许为了数字好看改评测集。

### 6.1 今天要给作品集攒的那一句话

跑完把这句填进 README 和作品集（数字按实测填，不许预估）：

> 「两阶段检索：BM25 + 智谱向量混合召回保证 **recall@3 = 100%**，
> Rerank 精排将 **P@1 从 75% 提升至 X%**，MRR 从 Y 提升至 Z；
> 通过意图路由，仅对语义类 query 启用 LLM 精排，成本控制在 ¥N/千次查询。」

**这句话命中 JD 三处原文**：混合检索 ✅、重排序 ✅、成本控制 ✅。

---

## 七、任务 ⑤ 落地 + 记忆（12 分钟）

**📋 可复制指令**：

```
1. 择优接入：把胜出的 reranker 接进 workflow.py 的检索环节
   （要求：search() 同签名，调用点零改动；rerank 失败自动回退到原顺序）

2. 更新 README.md 第十三节：
   - 新增「Rerank 精排（Day04）」小节，含三方对比表 + 上面那句结论
   - 目录树加 src/reranker.py

3. 同步更新 作品集.html 的 RAG section + dist/index.html，重新部署
   （部署命令沿用你已验证的：先 unpublish 再 deploy 清缓存）

4. 写入工作区日志 .workbuddy/memory/2026-09-02.md：
   - 三方对比的完整数字（P@1 / MRR / recall@3 / 耗时 / 成本）
   - LLM 精排救回来的 query 与搞砸的 query 清单（含归因）
   - 意图路由的判定逻辑与阈值
   - 定时任务 9:01 今早是否出活（automation-1788145246236，昨天曾被 PAUSED）

5. 更新 .workbuddy/memory/MEMORY.md：RAG 链路已补齐 Rerank 环节
```

---

## 📦 今日交付清单

- [ ] 能口述「召回 vs 精排」的分工，说清为什么双塔必然漏语义
- [ ] **P@1 / MRR 精排基线已跑出**（P@1≈75%，recall@3=100%）
- [ ] `src/reranker.py` 已实现 FeatureReranker + LLMReranker
- [ ] LLM 精排**已优雅降级**（失败/超时回原顺序，不崩）
- [ ] **意图路由已生效**：数字类 query 跳过 LLM 精排
- [ ] `src/eval_rerank.py` 三方对比表已出（**今天的硬指标**）
- [ ] 胜出方案已接入 workflow，README / 作品集已更新并重新部署
- [ ] 记忆已写入

---

## ⚠️ 今日三条红线

1. **不许装 sentence-transformers / torch / FlagEmbedding / BGE-Reranker。**
   本地 rerank 模型要下几百 MB 权重，这台机器 venv 创建失败、`--target` 装 torch 极易翻车。
   一装，1.5 小时全耗在环境上，昨天 Day3 的教训白学了。
   **今天用 DeepSeek 做交叉编码——零依赖、1.5h 能跑完、效果不差。**

2. **recall@3 不许退化。** Rerank 是重排，不是过滤。
   候选池必须取 **top-10**（语料才 21 条），绝不能只 rerank top-3——那样上限被锁死，等于自欺欺人。

3. **评测集不许动。** 20 条 query 是 Day3 钉死的资产，改一条就跟前两天不可比，
   整个「BM25 baseline → 混合检索 → 精排」的演进故事就断了。

---

## 🗓️ 明日预告（9/3 周四 · 1.5h）

**RAG Day 4：上下文组装 + 生成，补上 RAG 的「G」**

| # | 任务 | 用时 | 前置 |
| --- | --- | --- | --- |
| 1 | 上下文组装（去重 / 截断 / 引用标注 / 来源可追溯） | 25min | 今天 top-1 准确率 |
| 2 | 接生成：检索结果 → prompt → 带引用的答案 | 30min | 无 |
| 3 | 忠实度 + 答案相关性评测（RAGAS 思路，自己实现不装包） | 25min | 无 |
| 4 | 幻觉检测：答案中的数字是否都能在 top-1 chunk 里找到 | 10min | 无 |

> 明天结束时你会拿到：**「检索 P@1 = X%，生成答案忠实度 = Y%，幻觉率 = Z%」**
> —— 这才是完整的 RAG 闭环，R 和 G 都有数字。

**🔴 今晚可做的 5 分钟前置**：无（今天不需要外部准备，Key 都已配好）。

---

## 💬 卡壳处理

| 问题 | 怎么办 |
| --- | --- |
| DeepSeek 调用超时 | 超时设 3s，超时直接回退原顺序——**降级路径本来就要写，顺手就测了** |
| LLM 输出的编号解析乱七八糟 | 正则抓所有数字 + 去重 + 缺失补尾，别追求一次解析完美 |
| P@1 提不上去 | **先查是不是候选池太小**，再看特征权重；实在不行记下来，明天用生成端补 |
| 时间不够 | 保 ④ 砍 ③ 的方案 A —— **对比数字 > 两种 reranker 都实现** |
| 卡住超 10 分钟 | 记下来跳过，继续下一项 |

---

## 📌 欠账提醒（本周六日清）

| 欠账 | 来源 | 预估 |
| --- | --- | --- |
| 封装 3 个自定义 Skill | Day2 降级 | 30min |
| 效率对比表（人工 vs AI 耗时） | Day2 降级 | 10min |
| 飞书案例 5 项待补数据（使用人数/自动化条数/前后对比/节省工时/成功案例） | 9/1 遗留 | 10min（问同事） |
| 3 分钟讲解视频 | L4 加分项 | 1h |

> 周六日共 8-12h，扣掉这些还剩大半，够做 Day5 的生成闭环 + 简历改造。

---

> 🎯 **今日一句话目标**：拿到 **三方对比的 P@1 / MRR 数字**。
> Reranker 写得漂不漂亮不重要，**这张表是你「RAG 全链路」故事的最后一块拼图**。
